# Base dependencies
# tkinter is part of Python's standard library

# File handling and compression
zipfile  # Part of Python's standard library
gzip     # Part of Python's standard library
struct   # Part of Python's standard library

# Testing dependencies (optional)
pytest>=7.0.0
pytest-cov>=4.0.0